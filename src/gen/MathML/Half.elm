
module MathML.Half exposing (view)

import Html exposing (math, text)
import VirtualDom exposing (nodeNS)
import Html.Attributes exposing (attribute)

-- this code was generated by latex2elm

node = nodeNS "http://www.w3.org/1998/Math/MathML"

view = math [Html.Attributes.attribute "display" "block"] [node "semantics" [  ] [ node "mrow" [  ] [ node "mtext" [  ] [ text "how does this look?" ], node "mfrac" [  ] [ node "mn" [  ] [ text "1" ], node "mn" [  ] [ text "2" ] ] ], node "annotation" [ attribute "encoding" "application/x-tex" ] [ text "\\text {how does this look?} \\frac{1}{2}" ] ]]
